import { TossLayout } from '@/components/layout/TossLayout';

export default function WinningStoresLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <TossLayout>{children}</TossLayout>;
}